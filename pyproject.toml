[project]
name = "connectlife-ha"
version = "0.25.0"
description = "Custom Home Assistant component for ConnectLife devices"
readme = "README.md"
requires-python = ">=3.13.2"
dependencies = [
    "connectlife==0.5.4",
]

[dependency-groups]
dev = [
    "jschon==0.11.1",
    "pyyaml>=6.0.3",
]
